#ifndef COMPUTER_H
#define COMPUTER_H
#include "Screen.h"
#include "Keyboard.h"

enum mainMenuOption {NOVA_AULA = 0, ADC_BIXO, RELATORIO, RESULTADO};

class Computer {
	public:
		void run(void);

	private:
		Keyboard keyboard;
		Screen screen;
		mainMenuOption displayMainMenu(void);
};


#endif
